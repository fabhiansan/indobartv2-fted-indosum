transformers>=4.30.0
torch>=2.0.0
datasets>=2.0.0 # For data loading and processing
tokenizers>=0.13.0 # For training custom tokenizer

# Add other potential needs like:
# sentencepiece # Often used by tokenizers
# accelerate # For distributed training / deepspeed
# tqdm # Progress bars
# numpy
# pandas
